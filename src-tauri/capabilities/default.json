{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the AI Video Editor desktop shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
